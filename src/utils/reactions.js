// Shared quick-reaction emoji set for both the whole-message reaction picker
// (MessageBubble.jsx) and the per-image reaction picker (MessageAttachments.jsx),
// so they always offer the same choices.
export const QUICK_REACTIONS = ['👍', '❤️', '😂', '😮', '😢'];
